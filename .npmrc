# .npmrc (optional, for internal/private registries)
# Safe to ignore unless you're working with private npm scopes or orgs.

# Use a custom registry (for private packages)
#registry=https://registry.npmjs.org/

# Custom registry for internal use (example)
# registry=https://mycompanyregistry.com/

# Auth token for accessing a private registry
# ->(starts from double slash) //mycompanyregistry.com/:_authToken=your-token-here
